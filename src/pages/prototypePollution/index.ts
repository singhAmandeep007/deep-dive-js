import { stringToHTML } from "utils";

export const prototypePollutionPage = () => {
  const htmlTemplate = /* HTML */ ` <h1>prototypePollutionPage</h1> `;
  return stringToHTML(htmlTemplate).node;
};
